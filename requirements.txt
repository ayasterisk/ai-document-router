# Core API
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7
PyYAML>=6.0

# PDF text extraction (born-digital)
pdfplumber>=0.11
pypdf>=5.0

# Gọi inference server (vLLM/TGI) — cho cả model định tuyến lẫn OCR VLM
httpx>=0.27

# OCR bản scan
#   - Qwen2.5-VL-7B qua vLLM (khuyến nghị): client chỉ cần pdf2image + Pillow.
#     pdf2image cần Poppler (pdftoppm) cài sẵn trên máy chạy.
#   - Tesseract offline (fallback): cần cài binary Tesseract + lang data "vie".
pdf2image>=1.17
Pillow>=10.0
# pytesseract>=0.3.13    # bỏ comment khi bật fallback Tesseract
