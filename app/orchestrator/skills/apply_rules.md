# Skill: apply_rules

Gọi tool `apply_rules` (rule engine deterministic) với 6 field + nội dung PDF.
Rule engine chạy rule cứng theo thứ tự ưu tiên và trả kết quả định tuyến.
Chỉ dùng model reasoning cho phần rule không match rõ ràng.
